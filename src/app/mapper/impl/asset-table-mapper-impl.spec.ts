import { AssetTableMapperImpl } from './asset-table-mapper-impl';

describe('AssetTable', () => {
  it('should create an instance', () => {
    expect(new AssetTableMapperImpl()).toBeTruthy();
  });
});
