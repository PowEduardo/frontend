import { AssetMovimentModel } from './asset-moviment-model';

describe('AssetMovimentModel', () => {
  it('should create an instance', () => {
    expect(new AssetMovimentModel()).toBeTruthy();
  });
});
