import { CardMovementModel } from './card-movement-model';

describe('CardMovementModel', () => {
  it('should create an instance', () => {
    expect(new CardMovementModel()).toBeTruthy();
  });
});
