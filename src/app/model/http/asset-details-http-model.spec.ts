import { AssetDetailsHttpModel } from './asset-details-http-model';

describe('AssetDetailsHttpModel', () => {
  it('should create an instance', () => {
    expect(new AssetDetailsHttpModel()).toBeTruthy();
  });
});
