import { MovimentRowMapper } from './moviment-row-mapper';

describe('MovimentRowMapper', () => {
  it('should create an instance', () => {
    expect(new MovimentRowMapper()).toBeTruthy();
  });
});
