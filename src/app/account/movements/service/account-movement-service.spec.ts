import { AccountMovementService } from './account-movement-service';

describe('AccountMovementService', () => {
  it('should create an instance', () => {
    expect(new AccountMovementService()).toBeTruthy();
  });
});
